# Stub: future framework tests
# Add subdirectories for each framework component under test when they exist.
# Example:
# if(EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/frame_work)
#     add_subdirectory(frame_work)
# endif()