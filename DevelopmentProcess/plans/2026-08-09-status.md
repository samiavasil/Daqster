# 2026-08-09 — Session Status

## Objective

User reported: Qt6 file playback shows green screen through both input types
(streaming + image); Qt5 works. Also: popup window should close automatically
on input disconnect.

## Diagnosis: Qt6 green screen = Qt FFmpeg-backend VC-1 decode bug (NOT our code)

- Test file `50MB_1080P_THETESTDATA.COM_AVI.avi`: codec VC-1 Advanced profile,
  container ASF (mislabelled .avi), yuv420p 8-bit, 1920x1080@60.
- Evidence chain (standalone probe using the same QMediaPlayer+QVideoSink
  pattern as VideoCompat, no Daqster presentation code):
  1. System FFmpeg CLI decodes file correctly (real content).
  2. Qt-bundled libavcodec 61 (FFmpeg 7.1) driven directly: real content,
     0/30 zero-chroma frames.
  3. H.264 via Qt6 backend: real content (pipeline proven sound).
  4. VC-1 remuxed to MKV via Qt6: same zero-chroma garbage (not container issue).
  5. VC-1 via Qt6 QFFmpegMediaPlugin: NV12 frames with ENTIRE chroma plane zero
     (Cb=Cr=0) → YCbCr→RGB = solid green (0,226,0). Both display paths (GPU
     QVideoWidget + software QLabel) show green because both consume the same
     garbage frames.
- Confirmed codec difference: `clipGolemDelivers.avi` (Cinepak rgb24) plays
  fine on Qt6. H.264 also fine.
- Qt5 works for VC-1 because it uses GStreamer backend.
- CONCLUSION: upstream Qt 6.9.2 bug in QFFmpegMediaPlugin for VC-1 Advanced
  content. Not fixable in Daqster code. Not a regression from recent
  optimizations (zero-chroma produced inside Qt's decoder; standalone probe
  reproduces it without any Daqster presentation code; PL-020 was verified only
  as "compiles + no crash" — pixels never checked).
- Known-issue note for changelog: "VC-1 Advanced content shows green screen on
  Qt6 (Qt FFmpeg backend zero-chroma decode bug; works on Qt5 via GStreamer).
  H.264/Cinepak work."

## Fix: popup closes on input disconnect (VERIFIED by user)

- Branch `fix/video-popup-close-and-qt6-green`, commit `d86b095`.
- Root cause: disconnecting the edge does NOT stop the source player — frames
  keep flowing into `setInData()` and lazily re-create the detached
  `QVideoWidget` (`ensureVideoWidget()`). Also `inputConnectionDeleted()` was
  never implemented.
- Fix: `m_videoInputConnected` flag via `inputConnectionCreated`/`Deleted`
  overrides; guard `setInData()` port-0 branch with it; on disconnect hide +
  deleteLater + nullptr the widget, reset state, restore QLabel placeholder.
  Defensive null-check before `presentFrame()`. Also stops emitting `ImageData`
  wrapping null `QImage`.
- Qt5 unaffected (`#if QT_VERSION >= 6`). Compiles on Qt5 + Qt6; ctest 5/5;
  nodes tests 11/11.
- User manually verified: disconnect now hides the window. ✔

## Merge decision (user directive)

- NO merges into master. Keep sequential branches:
  `feat/REQ-SW-PL-020-video-frame-display` →
  `feat/REQ-SW-PL-021-in-scene-video-display` →
  `fix/video-popup-close-and-qt6-green`. No push until user says so.
- NOTE: PL-020 implementation code is NOT on master (master `VideoOutputNode.cpp`
  is the pre-PL-020 simple version). The popup fix cannot be cherry-picked to
  master without PL-020 (references `m_videoWidget`/`VideoCompat` which don't
  exist there).

## Pending / follow-ups

- Temporary frame diagnostics (commit `abd46db` on fix branch) to be removed
  after testing phase — currently kept intentionally.
- Pre-existing unrelated uncommitted change in
  `src/plugins/requirements_manager/TraceabilityMatrixWidget.cpp` — left
  untouched, NOT committed.
- PL-020 AC 6 (unit tests) still deferred.
- PL-021 in-scene GPU video display: implementation in progress; inherits the
  same setVideoFrame re-present mechanism — the VC-1 Qt6 finding transfers
  there.

## Docs review

**Docs review: done** — video subsystem reviewed in depth this session.

---

## Addendum: REQ-SW-PL-022 (afternoon session)

- REQ-SW-PL-022 "Unified Sampled Data Transport & DAQ Display": created per RDD
  gate, IMPLEMENTED (8 commits on branch
  `feat/REQ-SW-PL-022-unified-sampled-data-transport-daq-display`), AC 1-8 done,
  unit tests DEFERRED per user standing instruction.
- What was built:
  1. **Qt6 silent-audio fix** — `QAudioOutput` + `setAudioOutput` (the
     `/tmp/h264_test.mp4` audio was silent before this).
  2. **`SampledData` type** `{"sample","Sample"}` QtCore-only (evolution of
     `GenericNumericData`; `AudioData` = `SampledData` domain="audio", no
     separate class).
  3. **`SampledStreamDescriptor`** — consolidation of `GenericStreamConfig` +
     `QDevIOStreamConfig` + `QAudioFormat`-via-`AudioCompat`; double sampleRate,
     widened SampleType int8..float64, endianness, unit/scale, domain
     discriminator, device id, timestamps.
  4. **Unified decoder convention** — 32767 + clamp [-1,1] (fixes the
     32768-vs-32767 inconsistency).
  5. **Audio output port appended LAST** on `VideoFileSourceNode` /
     `StreamSourceNode` (Qt6 port 2 / Qt5 port 1; Qt6 `QAudioBufferOutput`
     6.8+ verified / Qt5 `QAudioProbe`; EOS empty buffer ignored; wrap-only
     handler gated on connection count).
  6. **DAQ Display node** — DataPlot architecture with `std::function`
     preprocessing slots (JIT-ready per user), v1 identity + FFT, real Qt
     Charts rendering replacing the stubs (`GenericDisplayNode`
     updateTimeChart/updateFFTChart, `AudioDisplayModel` configureAudioView).
- Design decision documented: ONE unified type + domain field as runtime
  discriminator (DAQ Display = oscilloscope; user explicitly chose this over
  discriminated types).
- Verification: Qt5 (5.15.2) + Qt6 (6.9.2) builds PASS; ctest 5/5 both; headless
  AC 7 proof — synthetic 100Hz "vibration" sine through same port, 13/13 checks
  PASS, FFT peak exact bin; app smoke no crash. MANUAL test REMAINING:
  real-session play of `/tmp/h264_test.mp4` — audible sound + DAQ Display
  waveform/FFT.
- Out of scope (deferred): mic migration from QDevIO, AI audio
  (`AudioToTensorNode` private, needs real model), DAQ hardware nodes, JIT
  preprocessing compiler, zero-copy audio.
- Archive cleanup DONE: public requirements tree now has `archive/` (DONE files
  moved per RDD process).
- RDD-STATUS updated (Фаза 3.10, private repo, commit `a61aee1`). Traceability
  matrix updated (PL-022 row; note PL-021 row MISSING — follow-up). Changelogs
  BG/EN updated.
- Current branch chain (user directive: NO master merges, NO push):
  master (`3c8c47f`) → `feat/REQ-SW-PL-020-video-frame-display` (`706f414`) →
  `feat/REQ-SW-PL-021-in-scene-video-display` (`8f6b2fe`) →
  `fix/video-popup-close-and-qt6-green` (`f0f5021`) →
  `feat/REQ-SW-PL-022-unified-sampled-data-transport-daq-display` (`f2ca6ed`).
