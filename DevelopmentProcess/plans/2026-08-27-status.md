# 2026-08-27 — Session Status

## Objective

Довършване на документацията за бранч
`feat/REQ-SW-PL-028-032-video-frame-cache-combo` в публичното repo
(`/mnt/Builder/Projects/samiavasil/daqster`). Само документация, НИКАКЪВ код.

## Branch

`feat/REQ-SW-PL-028-032-video-frame-cache-combo`

## Всички комити днес (в хронологичен ред)

| Hash | Описание |
|------|----------|
| `d4a90ee` | nodeeditor submodule: onNodeDataArrived virtual + dataArrivalChangesGeometry |
| `8418f53` | CustomDataFlowScene override + 3 video nodes opt-out (repaint-only на data arrival) |
| `42ba334` | CustomShaderNode (REQ-SW-PL-029): processor + node + widget + registration |
| `0682c1b` | texture() → texture2D() compat shim (GLSL 120) |
| `d5145c2` | nPorts 2→3 (първоначален fix — Sample порт беше недостижим) |
| `8c9b827` | Документация: REQ-SW-PL-029 DONE, PL-032 AC 11, PL-022 amendment, CHANGELOG, traceability, RDD-STATUS, AGENT-KNOWLEDGE, session status |
| `408ed2c` | Code review fixes: dynamic_cast, destructor qWarning, #include QVector2D |
| `5ee7d11` | VideoFileSourceNode: 2 порта (video-frame + sample), без gap |
| `13b8299` | StreamSourceNode + CameraSourceNode: 2 порта (video-frame + sample) |
| `f26e677` | REQ-SW-PL-033 (Qt6 камера аудио, FUTURE) + INC-007 gap |

## Verification

- Qt5 build **PASS**, Qt6 build **PASS**
- ctest: 9/9 (повечето), 10/10 (при nPorts fix)
- CustomShaderNode потвърден работещ от потребителя на Qt6

## Текущо състояние

- Всички source нодове (VideoFileSourceNode, StreamSourceNode, CameraSourceNode)
  имат **2 порта**: port 0 = video-frame, port 1 = sample (audio)
- CameraSourceNode на Qt6: sample портът емитира invalid (Qt платформена
  лимитация — QAudioBufferOutput е playback-only, QAudioProbe е премахнат)
  → REQ-SW-PL-033 (FUTURE) + INC-007
- Scene invalidation fix: video нодовете ползват repaint-only път
  (`node->update()`), NumberDisplay/DaqDisplay запазват пълния път

## Pending / Известни неувързаности

- **Merge в develop — чака потребителско решение** (бранчът е пушнат на origin
  на 2026-08-27; merge НЕ е правен)
- nodeeditor submodule: локален бранч `feat/REQ-SW-PL-028-032-video-frame-cache-combo`
  (създаден от `deembed-hover-fronting-wm`), няма remote бранч — gitlink-ът
  (`566f6ba`) сочи към комит, който не е на никой remote; submodule не се
  force-push-ва
- REQ-SW-PL-032 AC 9 (Phase 2 equivalence — визуална оценка на ефектите) —
  чака потребителска оценка
- 2 MINOR code review findings не са адресирани: C-style arrays в
  CustomShaderGLProcessor; texture() shim не е в VideoGLShaders.h
- CustomShaderNode smoke driver (DAQSTER_AUTOSTART_SHADER=1) — предложен, не поискан
- INC-002: 4 legacy REQ файла (PL-022/023/024/025) в `active/` — **архивирани** в `archive/plugins/` (2026-08-27), INC-002 затворено

## Следващи стъпки

1. Merge в develop — чака потребителско решение
2. По желание: MINOR findings + smoke driver
3. Потребителска визуална оценка за AC 9

## REQ-SW-PL-034 — VideoOutputNode embedded effects (2026-08-27, комит `615f53e`)

- **Имплементирано:** Display нодът (`VideoOutputNode`) получи **опционални
  вградени ефекти** — същите 11 ефекта като `VideoEffectNode`, **без ефект по
  подразбиране** (комбобокс с водещ „No effect" index 0 + `m_effectEnabled`).
- **Zero-copy гаранция (AC 2):** при липса на ефект блокът в `setInData()`
  се пропуска изцяло — `asTexture()`/`asImage()` не се викат, display пътят е
  byte-identical на предишната версия.
- **Backend:** GPU path (`asTexture()` → `VideoEffectGLProcessor::processTexture()`
  → `fromTexture()`, GpuRgba zero-copy) при хардуерен GL; CPU path/fallback
  (`asImage()` → `EffectSpec::cpuApply` → `QVideoFrame`).
- **Persist:** `save()`/`load()` — `"effect"` id + параметри с clamp-ове;
  стари графи без `"effect"` ключ → „no effect" (backward compatible).
- **Тестове:** `demo_nodeditor_videooutput_tests` +4 (default no-effect
  passthrough, load→save round-trip, backward compat, ефектът трансформира
  frame-а) — 10/10 на Qt5 и Qt6; ctest 9/9 green (и двете).
- **Забележка (отклонение от плана):** `setEffectIndex()` приема **combo index**
  (0 = „No effect"); `load()` мапва `indexOfEffect(id)` → combo index `+1`
  (планът казваше `setEffectIndex(indexOfEffect(id))` без +1 — това би избрало
  грешен ефект, тъй като combo index 0 е „No effect").
- **Файлове:** `VideoOutputNode.{h,cpp}`, `test_video_output_node.{h,cpp}`,
  `tests/plugins/demo_nodeditor_nodes/CMakeLists.txt` (VideoEffectOps/
  VideoTransformOps/VideoEffectGLProcessor добавени в test target),
  `REQ-SW-PL-034-video-output-node-embedded-effects.md` (AC 1–5 `[x]`),
  CHANGELOG.md + CHANGELOG.en.md, traceability-matrix.md, RDD-STATUS.md.
- **Не е пушнато, не е merge-нато в develop** (бранч
  `feat/REQ-SW-PL-028-032-video-frame-cache-combo`).

## Docs review

**Docs review: done** — публична документация прегледана и актуализирана.

## Node body repaint opt-out (REQ-SW-PL-032, perf) — `dataArrivalChangesWidget()`

- **Имплементирано:** нов virtual `dataArrivalChangesWidget()` в nodeeditor
  (`NodeDelegateModel.hpp`, default `true`) — opt-out за per-frame `node->update()`
  repaint-а на тялото на нода при пристигане на данни.
- **nodeeditor submodule** (`43c1dea`): virtual + `BasicGraphicsScene::onNodeDataArrived`
  пропуска `node->update()` при opt-out (запазва geometry/embed/moveConnections);
  `CustomDataFlowScene` fast-path също проверява флага.
- **Opt-out нодове (return false):** `VideoOutputNode`, `VideoEffectNode`,
  `CustomShaderNode`, `FrameSamplerNode`, `LLamaModelDataModel`, `ConsoleDataModel`
  (public) + `LlamaGenerateModel`, `FrameToTensorNode` (private).
- **Запазват default-а (true):** NumberDisplay, DaqDisplay, GenericDisplay,
  Modulo, ArithmeticLogic, obsolete нодове — validation state зависи от данните.
- **Очакван ефект:** Case B (VideoEffect нод между source и output) CPU да падне
  до ~0-1% от Case A (преди: +3-5%).
- **Не е пушнато, не е merge-нато в develop.**

## Late session (след пушването на `267d02f`) — perf/zero-copy довършване

Всички комити от late session (в хронологичен ред, след `267d02f`):

| Hash | Описание |
|------|----------|
| `64c1448` | Repaint opt-out — `dataArrivalChangesWidget()` (6 public нода skip per-frame body repaint); nodeeditor submodule `43c1dea` |
| `8925708` | `setValidationState` self-invalidate — validation нодовете (Modulo, ArithmeticLogic, DaqDisplay, GenericDisplay, NumberDisplay) opt-out-ват също; nodeeditor submodule `566f6ba` |
| `f9bd7bb` | NumberDisplay — geometry update само при реален widget resize (`requestNodeUpdate` + event filter) |
| `f758cd8` | VideoOutputNode output zero-copy passthrough — `m_output = videoFrame`, без readback/re-wrap при прикачен consumer; double-present поправен |
| `accc8aa` | `presentYuvTexture` — reuse на кеширани YUV текстури, без duplicate upload (потребител: „резултатите са много по-добри") |
| `211034c` | `VideoFrameData` copy ctor deleted (move-only) |
| `460cbf1` | Dead code removal — `VideoEffectGLProcessor::process()` премахнат (185 реда, FBO readback trap) |
| `5bc3a89` | Документация: CHANGELOG (validation self-invalidate, NumberDisplay resize-only, zero-copy output, presentYuvTexture, copy-ctor delete, dead process()) |
| `cf5bf7c` | **Stage 2C** — GpuRgba frames ползват GL blit на Qt6: zero-copy, без per-sink RHI upload (потребител: 2 display-а на effect output вече са евтини) |
| (private `66eaf6e`) | FrameToTensorNode — единична YUV→RGB888 конверсия |

### Детайли

- **Repaint opt-out (`64c1448`):** виж секцията по-горе. 6 public нода
  (`VideoOutputNode`, `VideoEffectNode`, `CustomShaderNode`, `FrameSamplerNode`,
  `LLamaModelDataModel`, `ConsoleDataModel`) + private (`LlamaGenerateModel`,
  `FrameToTensorNode`). Submodule `43c1dea`.
- **setValidationState self-invalidate (`8925708`, submodule `566f6ba`):**
  validation нодовете (Modulo, ArithmeticLogic, DaqDisplay, GenericDisplay,
  NumberDisplay) също opt-out-ват от per-frame body repaint —
  `setValidationState` сам инвалидира при промяна на валидацията.
- **NumberDisplay (`f9bd7bb`):** geometry update само при реален widget resize
  (`requestNodeUpdate` + event filter) — без излишни geometry invalidations.
- **VideoOutputNode output zero-copy passthrough (`f758cd8`):** при прикачен
  consumer output портът връща `m_output = videoFrame` директно — без readback
  и re-wrap; double-present (двойно презентиране на кадър) поправен.
- **presentYuvTexture (`accc8aa`):** YUV текстурите се кешират и reuse-ват —
  без duplicate upload на кадър. Потребител потвърди визуално:
  „резултатите са много по-добри".
- **VideoFrameData copy ctor deleted (`211034c`):** GpuRgba копията бяха
  задънена улица — copy ctor-ът е изтрит (move-only).
- **Dead code removal (`460cbf1`):** `VideoEffectGLProcessor::process()` (185
  реда, FBO readback trap) премахнат.
- **Stage 2C (`cf5bf7c`):** GpuRgba frames на Qt6 се презентират с GL blit —
  zero-copy, без per-sink RHI upload. Потребител потвърди: 2 display-а на
  effect output вече са евтини.
- **FrameToTensorNode (private `66eaf6e`):** единична YUV→RGB888 конверсия
  (вместо двойна).

### Verification

- Qt5 build **PASS**, Qt6 build **PASS**
- ctest: 9/9 (public) + 10/10 (private)
- Потребител потвърди: performance е добър
- 3 нови unit теста: 2 private pixel-identity + 1 public
  `gpuRgbaRoutesToGlBlitWidget` — запазени по решение на потребителя
  („всичко е идеално")

### Pending

- Merge в develop — чака потребителско решение
- AC 9 (Phase 2 equivalence — визуална оценка на ефектите) — чака
  потребителска оценка
- nodeeditor submodule `566f6ba`: локален бранч, няма remote — gitlink-ът
  сочи към комит, който не е на никой remote (не се force-push-ва)

### Docs review

**Docs review: done** — публична документация прегледана и актуализирана.
