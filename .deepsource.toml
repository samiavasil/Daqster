# DeepSource configuration — currently DISABLED.
# The analyzer workflow is parked at .github/workflows/deepsource.yml.disabled;
# see the header comment there for how to re-enable it.
version = 1

exclude_patterns = [
  "build/**",
  "src/plugins/external_libs/**",
]

[[analyzers]]
name = "cxx"
enabled = true

  [analyzers.meta]
  compile_commands_path = "build/compile_commands.json"
